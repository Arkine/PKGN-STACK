{
  "name": "pegn",
  "version": "1.0.0",
  "description": "PEGN Stack",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start:client": "cd client && yarn dev",
    "start:server": "cd server && yarn start",
    "dev": "concurrently --raw --kill-others \"yarn run start:server\" \"yarn run start:client\""
  },
  "devDependencies": {
    "concurrently": "^3.5.1"
  },
  "dependencies": {
    "apollo-link-context": "^1.0.8",
    "graphql": "^0.13.2"
  }
}
